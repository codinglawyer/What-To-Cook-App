import React from 'react';

const RecipeDetail = ({ recipes }) => {
    return (
        <div>
            {/*{recipes.map(recipe => (*/}
                {/*<div key={recipe.id}>*/}
                    {/*{recipe.title}*/}
                {/*</div>*/}
            {/*))}*/}
        </div>
    )
};

export default RecipeDetail;
